import { FC } from 'react';
import { Container, Logo } from './styles';

const Navigation: FC = () => {
  return (
    <Container>
      <Logo>synx</Logo>
    </Container>
  );
};

export default Navigation;
